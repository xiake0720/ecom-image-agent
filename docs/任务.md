分阶段 Codex 提示词

下面这些提示词，我已经按“尽量不失控”的方式拆好了。
你不要一次全丢。按阶段，一个阶段完成并验收后，再执行下一个。
----------------------------------------------------------
阶段 0：项目收口与边界冻结
你现在是这个项目的资深全栈工程师兼架构师。请你基于当前仓库，先不要大规模写业务代码，先完成“项目收口”和“一期上线边界冻结”。

项目目标：
- 一期只保留这 5 个能力：
  1. 注册/登录
  2. 主图生成
  3. 详情图生成
  4. 历史任务记录与查看
  5. 单张图片局部标记后二次生成
- 暂不实现模板中心、资源库、数据中心、预览中心、系统设置这些 mock 页面

本次任务要求：
1. 深度阅读当前仓库目录与关键页面/服务
2. 输出一份 `docs/v1-scope-freeze.md`
3. 明确列出：
   - 一期保留页面
   - 一期隐藏页面
   - 一期保留 API
   - 一期废弃或冻结的旧实现
   - 风险点
   - 后续改造顺序
4. 在前端路由层增加 feature flag 或直接隐藏非一期页面入口，但不要删除代码
5. 不要改动主图/详情图核心生成逻辑
6. 若发现重复实现、旧 detail 逻辑、旧文档残留，请在文档中标记为 deprecated

输出要求：
- 提交代码
- 提交文档
- 给出变更摘要
- 给出后续建议

限制：
- 不要引入数据库
- 不要改动现有任务生成核心逻辑
- 不要做大范围重构
------------------------------------------
阶段 1：后端基础骨架与认证体系
你现在负责为当前项目补齐“一期可上线”的后端基础骨架。请严格在现有技术栈上增量实现，不要推翻现有主图/详情图生成能力。

目标：
- 引入 PostgreSQL + SQLAlchemy Async + Alembic
- 实现用户注册/登录/登出/刷新 token
- 建立 API v1 规范
- 建立统一配置、依赖注入、异常处理

本次任务必须完成：
1. 增加数据库基础设施：
   - `backend/db/session.py`
   - `backend/db/base.py`
   - `backend/db/models/`
   - Alembic 初始化
2. 增加以下表的最小版本：
   - users
   - refresh_tokens
   - audit_logs
   - idempotency_keys
3. 增加认证模块：
   - `POST /api/v1/auth/register`
   - `POST /api/v1/auth/login`
   - `POST /api/v1/auth/refresh`
   - `POST /api/v1/auth/logout`
   - `GET /api/v1/auth/me`
4. Access token 使用 JWT
5. Refresh token 使用 HttpOnly cookie
6. 密码使用安全哈希，不允许明文
7. 增加 `get_current_user` 依赖
8. 增加基础审计日志：注册、登录、登出
9. 补充 `.env.example`
10. 输出初始 Alembic migration

代码要求：
- 采用 Pydantic schema
- API 层 / service 层 / repository 层职责分离
- 类型标注完整
- 关键逻辑添加注释
- 不影响现有主图/详情图接口可运行性

完成标准：
- 本地能成功迁移数据库
- 能注册、登录、获取当前用户、刷新 token、登出
- 接口有基础错误处理
- 有最小单元测试或接口测试

限制：
- 先不要接入 COS
- 先不要改造现有 task 存储逻辑
- 先不要接入 Celery
---------------------------------------------------------------------------
阶段 2：任务数据模型与历史记录落库
你现在继续完善项目后端，把任务系统从本地 JSON/文件索引逐步迁移到数据库，但要保持兼容，避免直接破坏现有生成能力。

目标：
- 引入任务主表和结果表
- 支持历史任务查询
- 实现按用户隔离

本次任务必须完成：
1. 新增数据库表：
   - tasks
   - task_assets
   - task_results
   - task_events
   - task_usage_records
2. 任务类型枚举：
   - main_image
   - detail_page
   - image_edit
3. 任务状态枚举：
   - pending
   - queued
   - running
   - succeeded
   - failed
   - partial_failed
   - cancelled
4. 新增 API：
   - `GET /api/v1/tasks`
   - `GET /api/v1/tasks/{task_id}`
   - `GET /api/v1/tasks/{task_id}/runtime`
   - `GET /api/v1/tasks/{task_id}/results`
5. 所有任务查询必须按当前登录用户隔离
6. 为现有主图/详情图任务补充“写 DB 记录”的兼容层
7. 历史任务列表支持分页、task_type、status 过滤
8. task_events 记录关键状态变化
9. task_usage_records 先预留写入接口和模型，不要求完全接入所有 provider

要求：
- 尽量少改已有生成核心
- 先做兼容写入：旧逻辑继续跑，但任务元数据必须写数据库
- 给出迁移说明文档 `docs/task-migration-plan.md`

完成标准：
- 创建任务后能在历史任务接口查到
- 用户 A 不能看到用户 B 的任务
- 任务详情可返回 runtime 摘要和结果摘要
------------------------------------------------------------------------------------
阶段 3：腾讯云 COS 接入与文件安全上传
你现在负责把项目文件存储从本地磁盘逐步迁移到腾讯云 COS。请实现“一期可上线”的最小可用方案：私有桶 + 预签名上传 + 签名下载。

目标：
- 输入素材上传到 COS
- 生成结果图保存到 COS
- 数据库记录文件元数据
- 不再依赖本地磁盘作为正式存储

本次任务必须完成：
1. 增加 COS 配置项：
   - COS_SECRET_ID
   - COS_SECRET_KEY
   - COS_REGION
   - COS_BUCKET
   - COS_PUBLIC_HOST
   - COS_SIGN_EXPIRE_SECONDS
2. 新增服务：
   - `backend/services/storage/cos_service.py`
   - `backend/services/storage/upload_guard_service.py`
3. 新增 API：
   - `POST /api/v1/storage/presign`
   - `GET /api/v1/files/{file_id}/download-url`
4. 文件 key 规范统一为：
   - `users/{user_id}/tasks/{task_id}/{kind}/{filename}`
5. 上传前校验：
   - MIME 白名单
   - 文件大小限制
   - SHA256
6. 下载必须校验所有权后再签发 URL
7. `task_assets` / `task_results` 必须记录：
   - cos_key
   - mime_type
   - size_bytes
   - sha256
8. 前端接入直传
9. 更新 `.env.example`
10. 输出 `docs/cos-integration.md`

要求：
- 只实现图片上传
- COS 桶默认按私有读写设计
- 禁止把 Secret 下发到前端
- 尽量保留本地存储兼容开关，方便开发环境调试

完成标准：
- 前端可直传素材到 COS
- 结果下载通过后端签名 URL 返回
- 所有文件都能关联到用户和任务
--------------------------------------------------------------------------------------
阶段 4：Celery + Redis 改造异步任务系统
你现在负责把现有进程内任务执行方式改造成可上线的异步任务系统。必须基于 Redis + Celery 实现，但要尽量复用当前主图/详情图生成逻辑。

目标：
- 主图任务使用 Celery
- 详情图任务使用 Celery
- 任务状态写回数据库
- 失败可重试，异常可追踪

本次任务必须完成：
1. 增加：
   - `backend/workers/celery_app.py`
   - `backend/workers/tasks/main_image_tasks.py`
   - `backend/workers/tasks/detail_page_tasks.py`
2. 把现有主图生成入口改成：
   - 创建数据库任务
   - 推入 Celery
   - 返回 task_id
3. 把现有详情图生成入口改成：
   - 创建数据库任务
   - 推入 Celery
   - 返回 task_id
4. Worker 执行期间持续更新：
   - tasks.status
   - tasks.progress_percent
   - tasks.current_step
   - task_events
5. 失败时记录：
   - error message
   - retry count
6. 允许基础重试策略
7. Redis 配置与 Celery 配置进入 settings
8. 补充本地开发 docker-compose 服务
9. 输出 `docs/celery-task-architecture.md`

限制：
- 不要重写主图/详情图 prompt 生成逻辑
- 只做执行框架改造
- 保持原有业务输入输出结构尽可能兼容

完成标准：
- 主图和详情图任务都能异步运行
- 后端重启后，数据库中的任务记录仍可追踪
- 失败任务能正确标记，不会无声丢失
--------------------------------------------------------------------------------
阶段 5：前端真实化——登录、历史任务、任务恢复
你现在负责把前端从演示态改造成可上线的一期版本。只做真实页面，不要继续扩展 mock 页面能力。

目标：
- 登录注册真实可用
- 历史任务真实可用
- 主图/详情图任务可恢复
- 隐藏所有非一期 mock 页面

本次任务必须完成：
1. 改造 `LoginPage`
2. 新增 `RegisterPage`
3. 增加受保护路由
4. 增加 auth store / auth hooks
5. 历史任务页接入真实 API
6. 修复任务恢复逻辑：
   - main_image 任务恢复到主图页
   - detail_page 任务恢复到详情图页
7. 历史任务列表支持分页、筛选、状态展示
8. 任务详情可查看结果图和运行状态
9. 所有非一期页面从菜单隐藏
10. 输出 `docs/frontend-v1-pages.md`

要求：
- 不要重写 UI 风格
- 优先保证真实流程跑通
- 保证类型定义统一
- 清理明显无效的 mock 调用

完成标准：
- 用户登录后能创建任务、查看历史、恢复任务
- 用户刷新页面后认证状态正常
- 不会再错误地把详情图任务恢复到主图页面
----------------------------------------------------------------------------
阶段 6：单张图片二次优化与局部标记再生成
你现在负责实现一期最关键的增强能力：单张图片局部标记后二次生成。目标是做“最小可用版本”，不是做 Photoshop。

目标：
- 支持对单张结果图进行局部编辑
- 支持矩形选区或简单画笔遮罩
- 支持输入编辑指令
- 生成新的结果版本并关联原图

本次任务必须完成：
1. 新增表：
   - image_edits
2. 新增 API：
   - `POST /api/v1/results/{result_id}/edits`
   - `GET /api/v1/results/{result_id}/edits`
3. 前端新增组件：
   - 简单矩形选区
   - 简单遮罩涂抹（二选一先实现也可以，但代码结构需可扩展）
4. 用户可输入编辑指令，例如：
   - 修改局部文案
   - 优化某块视觉区域
   - 保留主体仅修改局部
5. 编辑任务走 Celery
6. 编辑结果写入：
   - image_edits
   - task_results（作为新版本或派生结果）
7. 在结果页支持查看：
   - 原图
   - 编辑后版本
8. 若当前 provider 不支持原生局部重绘，允许 fallback 到“全图受约束再生成”，但必须在代码里明确标记 mode
9. 输出 `docs/image-edit-v1.md`

要求：
- 不要做复杂图层系统
- 不要引入重量级前端绘图框架，除非必要
- 保证 API、DB、前端状态流清晰
- 给出可继续扩展到 brush/mask/inpainting 的结构

完成标准：
- 用户能在结果图上标记区域并输入指令
- 能生成新版本图
- 能查看编辑历史
-----------------------------------------------------------------------------
阶段 7：安全加固与上线准备
你现在负责把项目补到“一期可上线”的安全和运维标准。重点是最小上线必需项，不做过度设计。

目标：
- 补齐访问控制、限流、审计、配置安全、监控接入
- 输出上线清单和部署文件

本次任务必须完成：
1. 增加基础限流：
   - 登录接口限流
   - 任务创建接口限流
   - 上传签名接口限流
2. 增加基础审计：
   - 登录
   - 登出
   - 创建任务
   - 下载结果
   - 发起编辑
3. 增加统一异常日志
4. 增加健康检查与 readiness/liveness
5. 提供 docker-compose.dev.yml 与 docker-compose.prod.yml
6. 提供 Nginx 反向代理基础配置
7. 更新 `.env.example`
8. 输出：
   - `docs/deployment.md`
   - `docs/security-checklist.md`
   - `docs/release-checklist.md`

最好补充：
- Prometheus metrics 暴露
- 基础 Grafana dashboard 模板
- 文件上传大小限制
- CORS 白名单配置
- 安全响应头

完成标准：
- 项目可以通过 docker compose 启动基础依赖
- 有上线前检查清单
- 有最小监控入口
-----------------------------------------------------------------------------------
阶段 8：最终联调与修复收尾
你现在作为 release engineer，对整个一期项目做联调收尾。不要再做大功能扩展，只做修复、补漏、验证和文档收尾。

目标：
- 保证项目整体可运行
- 修复阻塞上线的问题
- 补充必要测试和脚本

本次任务必须完成：
1. 全链路自查：
   - 注册
   - 登录
   - 主图生成
   - 详情图生成
   - 历史任务
   - 下载
   - 单图编辑
2. 修复联调中发现的阻塞问题
3. 输出缺陷清单与剩余风险
4. 补充必要脚本：
   - migrate
   - create-admin（可选）
   - seed-demo-data（可选）
5. 整理 README：
   - 本地启动
   - 环境变量
   - 数据迁移
   - Worker 启动
   - COS 配置
6. 给出一期发布建议和二期建议

要求：
- 不做无关重构
- 不引入新依赖，除非修复阻塞问题必需
- 最终输出以“项目可运行”为第一目标

完成标准：
- 提供明确启动步骤
- 项目关键链路可跑通
- 文档能指导他人部署
八、给 Codex 的总控规则

这个你每次都可以附在提示词前面，避免它失控。

通用执行规则：
1. 先阅读当前仓库，再开始改代码
2. 优先增量改造，不要推翻现有主图/详情图生成链路
3. 不要一次做超大范围重构
4. 所有新增目录和文件路径必须统一规范
5. 所有 API、schema、model、service 命名必须一致
6. 关键逻辑必须写注释
7. 每次任务结束必须输出：
   - 改动文件列表
   - 变更摘要
   - 风险点
   - 下一步建议
8. 如发现旧实现冲突，不要直接删除，先标记 deprecated 或隐藏入口
9. 优先保证项目能运行，再追求结构完美
10. 若改动会影响现有主图/详情图核心逻辑，必须先说明兼容策略




-----------------------------------------------------------------------------------------
补充要求：请按 PostgreSQL + SQLAlchemy Async + Alembic 实现以下详细表结构，不要自行简化字段，不要把强关系字段偷放进 JSON。

阶段1表：
1. users
- id: UUID, PK
- email: VARCHAR(255), NOT NULL, UNIQUE
- password_hash: VARCHAR(255), NOT NULL
- nickname: VARCHAR(100), NULL
- avatar_url: VARCHAR(500), NULL
- status: VARCHAR(32), NOT NULL, default 'active'
- email_verified: BOOLEAN, NOT NULL, default false
- last_login_at: TIMESTAMPTZ, NULL
- last_login_ip: INET, NULL
- created_at: TIMESTAMPTZ, NOT NULL, default now()
- updated_at: TIMESTAMPTZ, NOT NULL, default now()
- deleted_at: TIMESTAMPTZ, NULL

2. refresh_tokens
- id: UUID, PK
- user_id: UUID, FK -> users.id, NOT NULL
- token_hash: VARCHAR(255), NOT NULL, UNIQUE
- device_id: VARCHAR(128), NULL
- user_agent: VARCHAR(500), NULL
- ip_address: INET, NULL
- expires_at: TIMESTAMPTZ, NOT NULL
- revoked_at: TIMESTAMPTZ, NULL
- replaced_by_token_id: UUID, FK -> refresh_tokens.id, NULL
- created_at: TIMESTAMPTZ, NOT NULL, default now()
- updated_at: TIMESTAMPTZ, NOT NULL, default now()

3. audit_logs
- id: UUID, PK
- user_id: UUID, FK -> users.id, NULL
- action: VARCHAR(100), NOT NULL
- object_type: VARCHAR(50), NULL
- object_id: UUID, NULL
- request_id: VARCHAR(64), NULL
- ip_address: INET, NULL
- user_agent: VARCHAR(500), NULL
- payload: JSONB, NULL
- created_at: TIMESTAMPTZ, NOT NULL, default now()

4. idempotency_keys
- id: UUID, PK
- user_id: UUID, FK -> users.id, NOT NULL
- request_key: VARCHAR(128), NOT NULL
- request_hash: VARCHAR(128), NOT NULL
- endpoint: VARCHAR(255), NOT NULL
- response_status: INTEGER, NULL
- response_body: JSONB, NULL
- expires_at: TIMESTAMPTZ, NOT NULL
- created_at: TIMESTAMPTZ, NOT NULL, default now()
- unique(user_id, request_key)

阶段2表：
1. tasks
- id: UUID, PK
- user_id: UUID, FK -> users.id, NOT NULL
- task_type: VARCHAR(32), NOT NULL
- status: VARCHAR(32), NOT NULL, default 'pending'
- title: VARCHAR(255), NULL
- platform: VARCHAR(50), NULL
- biz_id: VARCHAR(100), NULL
- source_task_id: UUID, FK -> tasks.id, NULL
- parent_task_id: UUID, FK -> tasks.id, NULL
- current_step: VARCHAR(100), NULL
- progress_percent: NUMERIC(5,2), NOT NULL, default 0
- input_summary: JSONB, NULL
- params: JSONB, NULL
- runtime_snapshot: JSONB, NULL
- result_summary: JSONB, NULL
- error_code: VARCHAR(100), NULL
- error_message: TEXT, NULL
- retry_count: INTEGER, NOT NULL, default 0
- started_at: TIMESTAMPTZ, NULL
- finished_at: TIMESTAMPTZ, NULL
- created_at: TIMESTAMPTZ, NOT NULL, default now()
- updated_at: TIMESTAMPTZ, NOT NULL, default now()
- deleted_at: TIMESTAMPTZ, NULL

2. task_assets
- id: UUID, PK
- task_id: UUID, FK -> tasks.id, NOT NULL
- user_id: UUID, FK -> users.id, NOT NULL
- role: VARCHAR(50), NOT NULL
- source_type: VARCHAR(50), NOT NULL
- source_task_result_id: UUID, FK -> task_results.id, NULL
- file_name: VARCHAR(255), NULL
- cos_key: VARCHAR(500), NOT NULL
- mime_type: VARCHAR(100), NOT NULL
- size_bytes: BIGINT, NOT NULL
- sha256: CHAR(64), NOT NULL
- width: INTEGER, NULL
- height: INTEGER, NULL
- duration_ms: INTEGER, NULL
- scan_status: VARCHAR(32), NOT NULL, default 'pending'
- metadata: JSONB, NULL
- sort_order: INTEGER, NOT NULL, default 0
- created_at: TIMESTAMPTZ, NOT NULL, default now()
- updated_at: TIMESTAMPTZ, NOT NULL, default now()

3. task_results
- id: UUID, PK
- task_id: UUID, FK -> tasks.id, NOT NULL
- user_id: UUID, FK -> users.id, NOT NULL
- result_type: VARCHAR(50), NOT NULL
- page_no: INTEGER, NULL
- shot_no: INTEGER, NULL
- version_no: INTEGER, NOT NULL, default 1
- parent_result_id: UUID, FK -> task_results.id, NULL
- status: VARCHAR(32), NOT NULL, default 'succeeded'
- cos_key: VARCHAR(500), NOT NULL
- mime_type: VARCHAR(100), NOT NULL
- size_bytes: BIGINT, NOT NULL
- sha256: CHAR(64), NOT NULL
- width: INTEGER, NULL
- height: INTEGER, NULL
- prompt_plan: JSONB, NULL
- prompt_final: JSONB, NULL
- render_meta: JSONB, NULL
- qc_status: VARCHAR(32), NULL
- qc_score: NUMERIC(5,2), NULL
- is_primary: BOOLEAN, NOT NULL, default true
- created_at: TIMESTAMPTZ, NOT NULL, default now()
- updated_at: TIMESTAMPTZ, NOT NULL, default now()

4. task_events
- id: UUID, PK
- task_id: UUID, FK -> tasks.id, NOT NULL
- user_id: UUID, FK -> users.id, NOT NULL
- event_type: VARCHAR(50), NOT NULL
- level: VARCHAR(20), NOT NULL, default 'info'
- step: VARCHAR(100), NULL
- message: TEXT, NOT NULL
- payload: JSONB, NULL
- created_at: TIMESTAMPTZ, NOT NULL, default now()

5. task_usage_records
- id: UUID, PK
- task_id: UUID, FK -> tasks.id, NOT NULL
- user_id: UUID, FK -> users.id, NOT NULL
- provider_type: VARCHAR(50), NOT NULL
- provider_name: VARCHAR(100), NOT NULL
- model_name: VARCHAR(100), NULL
- action_name: VARCHAR(100), NOT NULL
- request_units: INTEGER, NULL
- prompt_tokens: INTEGER, NULL
- completion_tokens: INTEGER, NULL
- image_count: INTEGER, NULL
- latency_ms: INTEGER, NULL
- cost_amount: NUMERIC(12,4), NULL
- cost_currency: VARCHAR(10), NOT NULL, default 'CNY'
- success: BOOLEAN, NOT NULL, default true
- error_code: VARCHAR(100), NULL
- metadata: JSONB, NULL
- created_at: TIMESTAMPTZ, NOT NULL, default now()

请同时完成：
1. SQLAlchemy models
2. Alembic migration
3. 必要索引
4. 外键约束
5. updated_at 自动更新方案
6. docs/database-schema-v1.md

注意：
- 所有任务相关表必须带 user_id
- 不要把文件二进制存数据库
- 不要把 task_type / status 做成随意字符串常量，统一集中定义
- 不要删除现有逻辑，只先补齐 DB schema 和 repository 层