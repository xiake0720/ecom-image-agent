Metadata-Version: 2.4
Name: ecom-image-agent
Version: 0.2.0
Summary: FastAPI + React e-commerce image generation workspace.
Requires-Python: <3.12,>=3.11
Description-Content-Type: text/markdown
Requires-Dist: streamlit>=1.44.0
Requires-Dist: langgraph>=0.3.18
Requires-Dist: pydantic>=2.8.0
Requires-Dist: pydantic-settings>=2.4.0
Requires-Dist: pillow>=10.4.0
Requires-Dist: PyYAML>=6.0.2
Requires-Dist: python-dotenv>=1.0.1
Requires-Dist: requests>=2.32.0
Requires-Dist: fastapi>=0.115.0
Requires-Dist: uvicorn>=0.30.0
Requires-Dist: python-multipart>=0.0.9
Provides-Extra: dev
Requires-Dist: pytest>=8.3.0; extra == "dev"
Requires-Dist: pytest-cov>=6.0.0; extra == "dev"

# ecom-image-agent（前后端分离版）

## 项目简介
本项目已从 Streamlit 单体演示升级为“FastAPI 后端 + React 前端”的可持续迭代结构。
当前目标是保证本地可运行、链路清晰、结构化落盘与可排查，不宣称生产可用。

## 目录结构
```text
backend/                 # FastAPI 后端（API、服务、模板、仓储）
frontend/                # React + TypeScript 工作台
backend/engine/          # 既有 LangGraph 工作流与生成引擎能力
backend/legacy/          # 历史 Streamlit 调试入口
outputs/tasks/           # 主图与详情任务产物目录
storage/tasks/index.json # 任务索引（本地 JSON）
docs/                    # 中文文档与 contract
```

## 快速启动（最小可运行）
### 1）安装 Python 依赖
```bash
pip install -e .
pip install fastapi uvicorn python-multipart
```

### 2）启动后端
```bash
uvicorn backend.main:app --reload --port 8000
```

### 3）启动前端
```bash
cd frontend
npm install
npm run dev
```

默认前端访问 `http://127.0.0.1:5173`，后端 API 基址 `http://127.0.0.1:8000/api`。

## 配置方式
通过环境变量（前缀 `ECOM_`）配置后端：
- `ECOM_API_PREFIX`：API 前缀，默认 `/api`
- `ECOM_STORAGE_ROOT`：任务索引和中间数据目录，默认 `storage`
- `ECOM_OUTPUTS_ROOT`：任务输出目录，默认 `outputs/tasks`
- `ECOM_TEMPLATE_ROOT`：模板目录，默认 `backend/templates`

## 主要能力
- 主图生成：复用既有 LangGraph 工作流，API 化接入。
- 详情页生成：模板驱动 + 数据驱动输出模块化 JSON。
- 任务记录：统一落盘到 `storage/tasks/index.json`，支持列表与详情查询。
- 模板管理：提供主图模板列表与详情页模板列表接口。

## Streamlit 状态说明
`backend/legacy/streamlit_app.py` 保留为历史调试入口，不再是主入口；正式运行请使用 FastAPI + React。
